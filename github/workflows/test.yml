name: Test
on: push 
  jobs:
  lint:
    runs-on : ubuntu-latest
    steps:
      --name:CHeckout
        users: actions/checkout@v4
      --name: Install
        run: npm ci
      --name: Lint 
        run: npm run lint

